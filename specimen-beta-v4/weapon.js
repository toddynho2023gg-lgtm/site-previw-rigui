// First-person pistol
const firstGun=pistolModel(.82);firstGun.position.set(.36,-.31,-.62);firstGun.rotation.set(-.08,Math.PI/2,.02);firstGun.visible=false;camera.add(firstGun);const muzzle=new THREE.PointLight(0xffbd68,0,2.4,2);muzzle.position.set(.68,.02,0);firstGun.add(muzzle);let recoil=0;
function reload(){if(!state.gun||state.ammo>=12||state.reserve<=0)return;const need=12-state.ammo,take=Math.min(need,state.reserve);state.ammo+=take;state.reserve-=take;updateHUD();beep(180,.05,.03)}
const ray=new THREE.Raycaster();function shoot(){if(!state.gun)return;if(state.ammo<=0){beep(90,.08,.04);reload();return}state.ammo--;recoil=.16;muzzle.intensity=8;setTimeout(()=>muzzle.intensity=0,45);gunSound();UI.cross.classList.add('fire');setTimeout(()=>UI.cross.classList.remove('fire'),80);ray.setFromCamera(new THREE.Vector2(0,0),camera);const hits=ray.intersectObject(monster,true);if(hits.length&&hits[0].distance<28&&!monsterDown){monsterHP--;monsterStun=.7;bloodBurst(hits[0].point);if(monsterHP<=0)downMonster()}updateHUD()}
function bloodBurst(p){for(let i=0;i<8;i++){const s=new THREE.Mesh(new THREE.SphereGeometry(.018+Math.random()*.025,5,4),new THREE.MeshBasicMaterial({color:0x740008}));s.position.copy(p);const v=new THREE.Vector3((Math.random()-.5)*2,Math.random()*1.7,(Math.random()-.5)*2);scene.add(s);let life=.7;particles.push({m:s,v,life})}}
const particles=[];
